require "spec_helper"

RSpec.describe MoneyChanger do
  it "has a version number" do
    expect(MoneyChanger::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
